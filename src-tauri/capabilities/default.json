{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability: core IPC plus the open-folder dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
